# SYSTEM ループ backlog ―『ともしび』ゲーム機構

> SYSTEM ループ(`loops.md`)の queue。**Tier順で上から処理**。1イテレーション1件 → gate緑 → 本欄更新。
> **Tier3(機構再設計)= 設計doc → ユーザ確認 → 実装**(凍結インバリアントを意図的に更新)。
> 安全網: `npm run gate`(`npm test` + smoke)。経済/進行/武器を変える項目は `tests/invariants.test.js` の凍結値を設計docに明記のうえ更新。
> done履歴・原票(プレイテストfeedback)は `feedback-backlog.md`(アーカイブ)に残す。本欄は現役キュー。

---

## Tier 3 ― 機構再設計(設計doc → ユーザ確認 → 実装)

### D2. 進行モデル変更(昇華数で解禁＋costW廃止＋昇華数で新イベント解放) — status: **design-done → ユーザ判断待ち(最重要)**
- 設計doc: `docs/progression-design.md`。§9に5決定点。中心=**昇華は遅い**→純昇華ゲートは進行遅延＋ブートストラップ問題。3案: A(二層・最安全)/**B(settled+昇華AND・おすすめ)**/C(純昇華・最大変更)。
- ✅ **costW廃止は先行実装済**(全章の解錠費撤去・読むだけで進む・金は戦闘/装備通貨に存続)。
- ⏳ **要ユーザ判断 = §9 #1 採用案(A/B/C)**。決まれば §11 順で実装(canPassGate に subl 条件→章ゲート更新→invariant更新→30日再シムで値確定→昇華数で新イベント解放→進行UI)。
- 依存: 「昇華数で新イベント解放(ローテ補充)」の解放先=章イベント(STORY側 ch3+で増加中)。

### D3. アイテムのランク1〜99(青天井)＋調合ランクアップ — status: todo(設計doc未着手)
- 仕様: 武器を**ランク1〜99青天井**化。ドロップは基本ランク1、**調合でまれにランク2〜3へ一気上げ**、確率は**物語進行で上昇**。
- 影響: `js/armory.js`・`data/weapons.js`・`js/ui.js`(武器庫)・経済(ランク→能力曲線)。凍結武器インバリアント更新。
- 次手: 設計doc `docs/item-rank-design.md`(ランク→ステ曲線・調合ランクアップ確率×進行・既存武器migrate)→ユーザ確認→実装。

## Tier 4+ ― ブリッジ受信・将来
- イテレーション冒頭で `bridge.md` の `## → System` を読みトリアージ(accept→本欄に優先度付き転記/reject)。
- 現在の `→ System` 未処理: 〔昇華/章ボス→原文修復率フック(med)〕〔feelings正答の差し色(low)〕(2026-06-21・要トリアージ)。

---
## 完了済(アーカイブは feedback-backlog.md)
- B1(costW不足ボタン無効化) / B2(複数語廃止→1語ずつ英文cloze) / B3({name}化) / B4(スマホ縦長) / B5(EXPバー) / P1(→D2吸収) / **D1(単語マスター=昇華・gate緑)**。
